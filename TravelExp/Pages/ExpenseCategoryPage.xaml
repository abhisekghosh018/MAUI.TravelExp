<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="TravelExp.Pages.ExpenseCategoryPage"
             xmlns:model="clr-namespace:TravelExp.Models"
             xmlns:vm="clr-namespace:TravelExp.ViewModels"
             Title="Expense Category"
             x:DataType="vm:ExpenseCategoryViewModel">
    
    
     <!-- Plus button top right-->
    <ContentPage.ToolbarItems>
        <ToolbarItem IconImageSource="plus.png" Command="{Binding AddCategoryCommand}"></ToolbarItem>
    </ContentPage.ToolbarItems>
    <!--<List of Expense Category -->
    <CollectionView ItemsSource="{Binding ExpenseCategories}" Margin="15">
        <CollectionView.ItemsLayout>
            <LinearItemsLayout Orientation="Vertical" ItemSpacing="0.5"></LinearItemsLayout>
        </CollectionView.ItemsLayout>
        <CollectionView.ItemTemplate>
            <DataTemplate x:DataType="model:ExpenseCategoryModel">
                <Border StrokeShape="RoundRectangle 4" Padding="10">
                    <Label Text="{Binding Name}"></Label>
                </Border>
            </DataTemplate>
        </CollectionView.ItemTemplate>
    </CollectionView>
    
</ContentPage>